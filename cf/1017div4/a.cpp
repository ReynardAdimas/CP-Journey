#include<bits/stdc++.h>
using namespace std; 

void solution()
{
   string a,b,c;
   cin >> a >> b >> c; 
   cout << a[0] << b[0] << c[0] << endl;
}   


int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(0);
    int t;
    //t = 1;
    cin >> t;
    while(t--)
    {
        solution();
    }
    return 0;
}