#include<bits/stdc++.h>
using namespace std;

bool cek(vector<int> d)
{

}

void solution()
{
   int n; 
   cin >> n; 
   set<int> s;
   vector<int> 
   bool cek = false; 
   for(int i=0;i<n;i++)
   {
     int x; 
     cin >> x;
     s.insert(x);  

     if(cek(s)) cout << "YES" << endl;
     else cout << "NO" << endl;
   }

}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(0);
    int t;
    t = 1;
    //cin >> t;
    while(t--)
    {
        solution();
    }

    return 0;
}