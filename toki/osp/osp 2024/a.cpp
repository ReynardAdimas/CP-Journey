#include<bits/stdc++.h>
using namespace std; 

void solution()
{
   string s; 
   cin >> s; 
   int n = s.size();
   for(int i=0;i<n;i++)
   {
    for(int j=i+1;j<n;j++)
    {
        for (int k = j+1; k < n; k++)
        {
            if(i=='o')
            {

            }
            else if(i=='s')
            {
                
            }
        }
        
    }
   }
}   


int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(0);
    int t;
    t = 1;
    //cin >> t;
    while(t--)
    {
        solution();
    }
    return 0;
}