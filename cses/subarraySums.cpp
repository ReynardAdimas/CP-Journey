#include<bits/stdc++.h>
using namespace std;

void solution()
{
   int n, x; 
   cin >> n >> x; 
   vector<int> v(n);
   for(int i=0;i<n;i++) cin >> v[i]; 
   for(int i=0;i<n;i++)
   {
    int temp = x; 
    for(int j=i+1;j<n;j++)
    {
        
    }
   } 
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(0);
    int t;
    t = 1;
    //cin >> t;
    while(t--)
    {
        solution();
    }

    return 0;
}